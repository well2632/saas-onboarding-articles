import CategoryPageSkeleton from '@/components/CategoryPageSkeleton';

export default function Loading() {
  // Você pode adicionar qualquer UI de fallback aqui ou apenas o skeleton
  return <CategoryPageSkeleton />;
}
